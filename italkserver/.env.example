# PostgreSQL connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# JWT secret for authentication (used by backend and NextAuth)
JWT_SECRET=your_jwt_secret

# --- Optional: Multer file storage path (if using local storage) ---
# STORAGE_PATH=./uploads

# --- CORS allowed origins (comma separated, optional) ---
# ALLOWED_ORIGINS=http://localhost:3000,https://italk-zeta.vercel.app